select
    player_name,
    country,
    season,
    split,
    games,
    winrate,
    kda,
    avg_kills,
    avg_deaths,
    avg_assists,
    csm,
    gpm,
    kp,
    dmg_pct,
    gold_pct,
    vision_pct,
    dpm,
    vspm,
    wpm,
    wcpm,
    vwpm,
    gd15,
    csd15,
    xpd15,
    fb_pct,
    fb_victim_pct,
    penta_kills,
    solo_kills
from {{ source('lol_staging', 'players') }}
where player_name is not null