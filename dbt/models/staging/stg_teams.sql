select
    team_name,
    region,
    season,
    split,
    games,
    winrate,
    kda,
    gpm,
    gdm,
    game_duration,
    first_pick_pct,
    blue_side_pct,
    kills_per_game,
    deaths_per_game,
    towers_killed,
    towers_lost,
    fb_pct,
    ft_pct,
    drags_per_game,
    drag_pct,
    vg_per_game,
    herald_pct,
    avg_drags15,
    td_at15,
    gd_at15,
    plates_per_game,
    baron_per_game,
    baron_pct,
    cspm,
    dpm,
    wpm,
    vision_wards_pm,
    wards_cleared_pm
from {{ source('lol_staging', 'teams') }}
where team_name is not null